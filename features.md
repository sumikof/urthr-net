# UrthrNet 機能バックログ（今後の実装一覧）

> このドキュメントは、UrthrNet で **今後実装が必要になる機能** を実行可能な単位に分解したバックログです。
> 各機能は将来、個別に `brainstorming → spec → plan → 実装` のサイクルに入ることを想定しています。
> ロードマップの俯瞰は [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md) を参照してください。

## 現状サマリ

- ✅ **サブプロジェクト #1: オンチェーン Protocol Core**（`programs/urthr-net/`）— **完了**。
  11命令（initialize / register / stake / unstake / create_campaign / fund / submit_claim /
  challenge / resolve / settle / close）、4アカウント（ProtocolConfig / Publisher / Campaign / Claim）。
  LiteSVM テスト 27本オールグリーン（実 SPL Token プログラムでフルライフサイクル検証）。
- ⬜ 本ドキュメントが対象とするのは、**コア強化（A）**・**オフチェーンサービス（B）**・
  **フロントエンド（C）** の未実装機能です。

## 各項目のフォーマット

各機能は次の項目で記述します。

- **ID** — 安定した識別子（例 `A1`）。将来の spec/plan からこのIDで参照します。
- **目的** — なぜこの機能が必要か。
- **受け入れ条件** — 「完了」と判断するための観測可能な条件（概要）。
- **依存関係** — 先に完了している必要のある他機能。
- **対応サブプロジェクト** — #1（コア）/ #2（オフチェーン）/ #3（フロント）。

優先度の目安: **P0**=次に着手すべき土台 / **P1**=MVP に必要 / **P2**=MVP後の強化。

> **デバッグパネル必須:** オンチェーン命令を伴う機能は、その受け入れ条件に「`app/src/debug/instructions/` に対応するデバッグパネル（新アカウント型なら `app/src/debug/accounts/` にインスペクタも）を追加し、`app/src/App.tsx` に配線、localnet で手動確認する」ことを **必ず含める**。パネルは送信前シミュレーション必須の `DebugPanel` 契約に従う（詳細は [`CLAUDE.md`](CLAUDE.md)）。

---

## A. プロトコルコア強化（オンチェーン・#1拡張）

### A1. 管理命令群（admin instructions） — P0 / ⚠️現状ギャップ

- **目的:** `ProtocolConfig` は `initialize_protocol` で各値を設定するが、**初期化後に変更する命令が一切存在しない**。
  特に `paused` フラグは緊急停止のために用意されているのに**切り替え手段が無く**、現状では一度も停止/再開できない。
  attestor の交代、手数料・最低ステーク額の調整も不可能。運用に必須。
- **受け入れ条件:**
  - `set_paused(paused: bool)`（admin署名）で `config.paused` を切り替えられる。（`set_paused` は実装済み。残りは未実装）
  - `update_attestor(new: Pubkey)`（admin署名）で attestor を交代できる。
  - `update_fee(bps: u16)`（admin署名）で手数料を変更できる。`bps <= FEE_DENOMINATOR` を検証。
  - `update_min_stake(amount: u64)`（admin署名）で最低ステーク額を変更できる。
  - すべて `has_one = admin` で権限を強制し、LiteSVM テストで権限拒否・境界値を網羅。
- **依存関係:** なし（#1 のみ）。
- **対応サブプロジェクト:** #1。

### A2. イベント発行（`emit!` ログ） — P0

- **目的:** 現状どの命令もイベントを発行しないため、オフチェーン側（Indexer / Reporter / ダッシュボード）が
  状態変化を**効率的に購読できない**。各命令で構造化イベントを発行し、インデックス・監視の土台にする。
- **受け入れ条件:**
  - 主要な状態遷移ごとにイベント定義（例 `CampaignCreated` / `CampaignFunded` / `ClaimSubmitted` /
    `ClaimChallenged` / `ClaimResolved` / `ClaimSettled` / `PublisherStaked` / `PublisherSlashed`）。
  - 各命令ハンドラの末尾で対応イベントを `emit!`。
  - イベントに後段が必要とするキー（campaign / publisher / claim_nonce / amount / status 等）を含む。
  - IDL にイベントが反映され、codegen クライアント（B0）から型付きで購読できる。
- **依存関係:** なし（#1 のみ）。B0/B1/B4 がこれを利用。
- **対応サブプロジェクト:** #1。

### A3. merkle プルーフによる per-event チャレンジ — P2

- **目的:** MVP では `Claim.merkle_root` を**保存するのみで未検証**。チャレンジャーが
  「この root に含まれる特定イベントが不正」と**個別イベント単位で証明**できるようにし、
  バッチ全体ではなく不正イベントだけを争えるようにする。
- **受け入れ条件:**
  - merkle proof を受け取り `merkle_root` に対して検証する命令／検証ロジック。
  - 不正イベント数に応じた部分スラッシュ／部分決済の会計ルール定義と実装。
  - 既存のトークン保存則（`escrow == budget_remaining + locked_budget`）を維持。
  - LiteSVM テストで proof 検証・部分スラッシュを網羅。
- **依存関係:** A1（運用命令）が望ましい。
- **対応サブプロジェクト:** #1。

### A4. 分散型裁定（単一 attestor の置換） — P2

- **目的:** 現状は単一の信頼された `attestor` が `resolve_claim` を独占。これを
  **投票委員会 / dispute game** に置き換え、信頼の分散と検閲耐性を実現する。
  トラストモデル上、コア状態機械は裁定方法から独立しているため**コアを作り直さず**に差し替え可能。
- **受け入れ条件:**
  - 裁定を多数決またはステーク加重投票で確定する仕組み（投票アカウント／集計）。
  - 裁定者の参加・離脱、誤判定への罰則（任意）。
  - `resolve_claim` を新メカニズムの確定結果で駆動するアダプタ。
  - LiteSVM テストで合意・不一致・タイムアウト経路を網羅。
- **依存関係:** A1、A2。
- **対応サブプロジェクト:** #1。

### A5. マルチ mint 対応 — P2

- **目的:** MVP は予算・担保ともに**単一 mint**（USDC 想定）。複数の決済通貨／担保通貨を
  扱えるようにする。スラッシュ時の通貨換算が絡むため価格オラクル等の設計が必要。
- **受け入れ条件:**
  - キャンペーン単位／プロトコル単位で許可 mint を管理。
  - 予算 mint と担保 mint が異なる場合のスラッシュ補填レート（オラクル or 固定）の設計と実装。
  - 既存の単一mint経路を壊さない（後方互換 or 明示的移行）。
- **依存関係:** A1。
- **対応サブプロジェクト:** #1。

### A6. Token-2022 / 守秘トランスファー対応 — P2

- **目的:** 現状は classic SPL Token のみ。Token-2022 拡張（手数料徴収・守秘トランスファー等）に対応し、
  広告主の予算額などの**機密性**を選択的に提供する。
- **受け入れ条件:**
  - `transfer_checked` 経路を Token-2022 プログラムでも動作させる（mint/プログラムIDの分岐）。
  - 守秘トランスファー拡張を使う場合の vault・命令設計。
  - LiteSVM テストで Token-2022 mint を用いた決済・スラッシュを検証。
- **依存関係:** A5 が望ましい。
- **対応サブプロジェクト:** #1。

---

## B. オフチェーンサービス（サブプロジェクト #2）

### B0. 共有クライアント SDK（IDL codegen） — P0

- **目的:** すべてのオフチェーンサービスとフロントエンドが、IDL から生成した**型付きクライアント**で
  プログラムと通信できるようにする。手書きの命令組み立てを排除し一貫性を担保。
- **受け入れ条件:**
  - `urthr_net` の IDL から `@solana/kit` ベースの型付きクライアントを生成（`clients/` 配下）。
  - 全11命令（＋A1で追加する管理命令）のビルダーと、4アカウントのデコーダを提供。
  - PDA 導出ヘルパ（config / publisher / stake_vault / campaign / escrow_vault / claim）。
  - 簡単なスモーク（命令組み立て＋デコード）のテスト。
- **依存関係:** A2（イベント購読を含めるなら）。コアIDLが必要。
- **対応サブプロジェクト:** #2。

### B1. Reporter サービス — P1

- **目的:** パブリッシャーのイベント（クリック/コンバージョン）を集計し、バッチで `submit_claim` を発行する。
  「skin in the game」として claim 提出時にパブリッシャー署名が必要なため、その署名フローを担う。
- **受け入れ条件:**
  - 生イベントの取り込み・集計（event_count と merkle_root の算出）。
  - 集計結果を `submit_claim` として送信（B0 利用、署名は安全に管理）。
  - 再送・冪等性（claim_nonce の扱い）と失敗時リトライ。
  - 単体テスト（集計ロジック）＋ surfnet 統合（任意）。
- **依存関係:** B0、A2。
- **対応サブプロジェクト:** #2。

### B2. Challenger サービス（Bot 検知） — P1

- **目的:** チャレンジ期間中に不正トラフィック（Bot 等）を検知し、`challenge_claim` を
  証拠ハッシュ付きで発行する。本プロジェクトのアドフラウド対策の中核。
- **受け入れ条件:**
  - 保留中 claim の監視（A2 イベント / B0 経由のポーリング）。
  - Bot 検知ロジック（ヒューリスティクス／モデルはプラガブル）と `evidence_hash` 生成。
  - チャレンジ期限内に `challenge_claim` を送信。
  - 検知ロジックの単体テスト。
- **依存関係:** B0、A2。
- **対応サブプロジェクト:** #2。

### B3. Attestor サービス — P1

- **目的:** チャレンジされた claim に対し、`resolve_claim(fraud)` で裁定（決済 or スラッシュ）を行う。
  MVP では単一の信頼された attestor として動作（将来 A4 で分散化）。
- **受け入れ条件:**
  - チャレンジ済み claim の監視と、証拠に基づく裁定判断のワークフロー。
  - `resolve_claim` の送信（attestor 鍵の安全管理）。
  - 裁定の監査ログ。
  - 単体テスト（判断ロジック）。
- **依存関係:** B0、A2。将来 A4 で置換。
- **対応サブプロジェクト:** #2。

### B4. Indexer サービス — P1

- **目的:** オンチェーン状態とイベントを購読し、ダッシュボードや分析が使いやすい形で
  読み取り API（または DB ビュー）を提供する。
- **受け入れ条件:**
  - A2 イベントの購読／アカウントのスナップショットを保存。
  - キャンペーン・パブリッシャー・claim の検索／集計クエリを提供する API。
  - 再編成（reorg）耐性と再同期。
  - 基本的な統合テスト。
- **依存関係:** B0、A2。
- **対応サブプロジェクト:** #2。

---

## C. フロントエンドダッシュボード（サブプロジェクト #3）

### C0. 共有ウォレット / 署名・トランザクション UX — P0

- **目的:** 既存の `app/`（ウォレット接続が確認済み）を拡張し、両ダッシュボードが共有する
  署名・送信・確認の UX 基盤を整える。送信前シミュレーションと明示的な承認を含む。
- **受け入れ条件:**
  - B0 クライアントを `app/` に統合し、ウォレット署名で命令を送信できる。
  - 送信前に `simulateTransaction` 結果（宛先・金額・mint・手数料・cluster）を表示し承認を得る。
  - 既定は devnet/localnet。エラー・確認待ち・成功のUI状態。
  - コンポーネント／フックのテスト（モック署名）。
- **依存関係:** B0。
- **対応サブプロジェクト:** #3。

### C1. 広告主ダッシュボード — P1

- **目的:** 広告主がキャンペーンを作成・資金投入・監視できる画面。
- **受け入れ条件:**
  - `create_campaign` / `fund_campaign` / `close_campaign` を UI から実行。
  - 予算残・ロック中予算・claim 状況（保留/チャレンジ/決済/スラッシュ）の可視化（B4 利用）。
  - 残予算の引き出し（close）導線。
  - 主要フローの UI テスト。
- **依存関係:** C0、B4。
- **対応サブプロジェクト:** #3。

### C2. パブリッシャーダッシュボード — P1

- **目的:** パブリッシャーが登録・ステーク・収益確認・チャレンジ状況を管理できる画面。
- **受け入れ条件:**
  - `register_publisher` / `stake` / `unstake` を UI から実行。
  - ステーク額・ロック額・最低ステーク要件・収益（決済済み）とスラッシュ履歴の可視化（B4 利用）。
  - 自分の claim のチャレンジ状況と challenge_deadline の表示。
  - 主要フローの UI テスト。
- **依存関係:** C0、B4。
- **対応サブプロジェクト:** #3。

---

## 推奨実装順序

依存関係から、おおむね次の順序を推奨します（コア強化 A3–A6 は post-MVP として並行可能）。

```
A1 (管理命令) ─┬─► A2 (イベント) ─► B0 (SDK) ─┬─► B1 Reporter
               │                              ├─► B2 Challenger
               │                              ├─► B3 Attestor
               │                              └─► B4 Indexer ─┬─► C1 広告主
               │                                              └─► C2 パブリッシャー
               │                    B0 ─► C0 (Tx UX) ─────────┘
               │
   post-MVP 強化（並行可）: A3 merkle / A4 分散裁定 / A5 マルチmint / A6 Token-2022
```

1. **P0 土台:** `A1`（管理命令・緊急停止の有効化）→ `A2`（イベント）→ `B0`（SDK）/ `C0`（Tx UX）。
2. **P1 MVP:** `B1`–`B4`（オフチェーン）→ `C1`/`C2`（ダッシュボード）。
3. **P2 強化:** `A3`–`A6`（プロトコル堅牢化）。

各機能は着手時に改めて `brainstorming → spec → plan → 実装` のサイクルで詳細化します。
