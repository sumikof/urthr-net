/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const PUBLISHER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  86, 152, 93, 215, 234, 89, 232, 104,
]);

export function getPublisherDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(PUBLISHER_DISCRIMINATOR);
}

export type Publisher = {
  discriminator: ReadonlyUint8Array;
  authority: Address;
  stakeVault: Address;
  stakedAmount: bigint;
  lockedAmount: bigint;
  metadata: ReadonlyUint8Array;
  bump: number;
};

export type PublisherArgs = {
  authority: Address;
  stakeVault: Address;
  stakedAmount: number | bigint;
  lockedAmount: number | bigint;
  metadata: ReadonlyUint8Array;
  bump: number;
};

/** Gets the encoder for {@link PublisherArgs} account data. */
export function getPublisherEncoder(): FixedSizeEncoder<PublisherArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["authority", getAddressEncoder()],
      ["stakeVault", getAddressEncoder()],
      ["stakedAmount", getU64Encoder()],
      ["lockedAmount", getU64Encoder()],
      ["metadata", fixEncoderSize(getBytesEncoder(), 32)],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: PUBLISHER_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Publisher} account data. */
export function getPublisherDecoder(): FixedSizeDecoder<Publisher> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["authority", getAddressDecoder()],
    ["stakeVault", getAddressDecoder()],
    ["stakedAmount", getU64Decoder()],
    ["lockedAmount", getU64Decoder()],
    ["metadata", fixDecoderSize(getBytesDecoder(), 32)],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link Publisher} account data. */
export function getPublisherCodec(): FixedSizeCodec<PublisherArgs, Publisher> {
  return combineCodec(getPublisherEncoder(), getPublisherDecoder());
}

export function decodePublisher<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Publisher, TAddress>;
export function decodePublisher<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Publisher, TAddress>;
export function decodePublisher<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Publisher, TAddress> | MaybeAccount<Publisher, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getPublisherDecoder(),
  );
}

export async function fetchPublisher<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Publisher, TAddress>> {
  const maybeAccount = await fetchMaybePublisher(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybePublisher<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Publisher, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodePublisher(maybeAccount);
}

export async function fetchAllPublisher(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Publisher>[]> {
  const maybeAccounts = await fetchAllMaybePublisher(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybePublisher(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Publisher>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodePublisher(maybeAccount));
}

export function getPublisherSize(): number {
  return 121;
}
