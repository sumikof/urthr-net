/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { URTHR_NET_PROGRAM_ADDRESS } from "../programs";

export const CHALLENGE_CLAIM_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [231, 213, 62, 171, 50, 56, 168, 63],
);

export function getChallengeClaimDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CHALLENGE_CLAIM_DISCRIMINATOR,
  );
}

export type ChallengeClaimInstruction<
  TProgram extends string = typeof URTHR_NET_PROGRAM_ADDRESS,
  TAccountChallenger extends string | AccountMeta<string> = string,
  TAccountClaim extends string | AccountMeta<string> = string,
  TAccountPublisher extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountChallenger extends string
        ? WritableSignerAccount<TAccountChallenger> &
            AccountSignerMeta<TAccountChallenger>
        : TAccountChallenger,
      TAccountClaim extends string
        ? WritableAccount<TAccountClaim>
        : TAccountClaim,
      TAccountPublisher extends string
        ? ReadonlyAccount<TAccountPublisher>
        : TAccountPublisher,
      ...TRemainingAccounts,
    ]
  >;

export type ChallengeClaimInstructionData = {
  discriminator: ReadonlyUint8Array;
  evidenceHash: ReadonlyUint8Array;
};

export type ChallengeClaimInstructionDataArgs = {
  evidenceHash: ReadonlyUint8Array;
};

export function getChallengeClaimInstructionDataEncoder(): FixedSizeEncoder<ChallengeClaimInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["evidenceHash", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: CHALLENGE_CLAIM_DISCRIMINATOR }),
  );
}

export function getChallengeClaimInstructionDataDecoder(): FixedSizeDecoder<ChallengeClaimInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["evidenceHash", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getChallengeClaimInstructionDataCodec(): FixedSizeCodec<
  ChallengeClaimInstructionDataArgs,
  ChallengeClaimInstructionData
> {
  return combineCodec(
    getChallengeClaimInstructionDataEncoder(),
    getChallengeClaimInstructionDataDecoder(),
  );
}

export type ChallengeClaimInput<
  TAccountChallenger extends string = string,
  TAccountClaim extends string = string,
  TAccountPublisher extends string = string,
> = {
  challenger: TransactionSigner<TAccountChallenger>;
  claim: Address<TAccountClaim>;
  publisher: Address<TAccountPublisher>;
  evidenceHash: ChallengeClaimInstructionDataArgs["evidenceHash"];
};

export function getChallengeClaimInstruction<
  TAccountChallenger extends string,
  TAccountClaim extends string,
  TAccountPublisher extends string,
  TProgramAddress extends Address = typeof URTHR_NET_PROGRAM_ADDRESS,
>(
  input: ChallengeClaimInput<
    TAccountChallenger,
    TAccountClaim,
    TAccountPublisher
  >,
  config?: { programAddress?: TProgramAddress },
): ChallengeClaimInstruction<
  TProgramAddress,
  TAccountChallenger,
  TAccountClaim,
  TAccountPublisher
> {
  // Program address.
  const programAddress = config?.programAddress ?? URTHR_NET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    challenger: { value: input.challenger ?? null, isWritable: true },
    claim: { value: input.claim ?? null, isWritable: true },
    publisher: { value: input.publisher ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("challenger", accounts.challenger),
      getAccountMeta("claim", accounts.claim),
      getAccountMeta("publisher", accounts.publisher),
    ],
    data: getChallengeClaimInstructionDataEncoder().encode(
      args as ChallengeClaimInstructionDataArgs,
    ),
    programAddress,
  } as ChallengeClaimInstruction<
    TProgramAddress,
    TAccountChallenger,
    TAccountClaim,
    TAccountPublisher
  >);
}

export type ParsedChallengeClaimInstruction<
  TProgram extends string = typeof URTHR_NET_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    challenger: TAccountMetas[0];
    claim: TAccountMetas[1];
    publisher: TAccountMetas[2];
  };
  data: ChallengeClaimInstructionData;
};

export function parseChallengeClaimInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedChallengeClaimInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      challenger: getNextAccount(),
      claim: getNextAccount(),
      publisher: getNextAccount(),
    },
    data: getChallengeClaimInstructionDataDecoder().decode(instruction.data),
  };
}
