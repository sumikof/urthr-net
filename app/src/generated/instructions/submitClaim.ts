/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda, findSubmitClaimPublisherPda } from "../pdas";
import { URTHR_NET_PROGRAM_ADDRESS } from "../programs";

export const SUBMIT_CLAIM_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  163, 108, 111, 46, 220, 82, 77, 212,
]);

export function getSubmitClaimDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SUBMIT_CLAIM_DISCRIMINATOR,
  );
}

export type SubmitClaimInstruction<
  TProgram extends string = typeof URTHR_NET_PROGRAM_ADDRESS,
  TAccountPublisherAuthority extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountPublisher extends string | AccountMeta<string> = string,
  TAccountCampaign extends string | AccountMeta<string> = string,
  TAccountClaim extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPublisherAuthority extends string
        ? WritableSignerAccount<TAccountPublisherAuthority> &
            AccountSignerMeta<TAccountPublisherAuthority>
        : TAccountPublisherAuthority,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountPublisher extends string
        ? WritableAccount<TAccountPublisher>
        : TAccountPublisher,
      TAccountCampaign extends string
        ? WritableAccount<TAccountCampaign>
        : TAccountCampaign,
      TAccountClaim extends string
        ? WritableAccount<TAccountClaim>
        : TAccountClaim,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SubmitClaimInstructionData = {
  discriminator: ReadonlyUint8Array;
  eventCount: bigint;
  merkleRoot: ReadonlyUint8Array;
};

export type SubmitClaimInstructionDataArgs = {
  eventCount: number | bigint;
  merkleRoot: ReadonlyUint8Array;
};

export function getSubmitClaimInstructionDataEncoder(): FixedSizeEncoder<SubmitClaimInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["eventCount", getU64Encoder()],
      ["merkleRoot", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: SUBMIT_CLAIM_DISCRIMINATOR }),
  );
}

export function getSubmitClaimInstructionDataDecoder(): FixedSizeDecoder<SubmitClaimInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["eventCount", getU64Decoder()],
    ["merkleRoot", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getSubmitClaimInstructionDataCodec(): FixedSizeCodec<
  SubmitClaimInstructionDataArgs,
  SubmitClaimInstructionData
> {
  return combineCodec(
    getSubmitClaimInstructionDataEncoder(),
    getSubmitClaimInstructionDataDecoder(),
  );
}

export type SubmitClaimAsyncInput<
  TAccountPublisherAuthority extends string = string,
  TAccountConfig extends string = string,
  TAccountPublisher extends string = string,
  TAccountCampaign extends string = string,
  TAccountClaim extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  publisherAuthority: TransactionSigner<TAccountPublisherAuthority>;
  config?: Address<TAccountConfig>;
  publisher?: Address<TAccountPublisher>;
  campaign: Address<TAccountCampaign>;
  claim: Address<TAccountClaim>;
  systemProgram?: Address<TAccountSystemProgram>;
  eventCount: SubmitClaimInstructionDataArgs["eventCount"];
  merkleRoot: SubmitClaimInstructionDataArgs["merkleRoot"];
};

export async function getSubmitClaimInstructionAsync<
  TAccountPublisherAuthority extends string,
  TAccountConfig extends string,
  TAccountPublisher extends string,
  TAccountCampaign extends string,
  TAccountClaim extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof URTHR_NET_PROGRAM_ADDRESS,
>(
  input: SubmitClaimAsyncInput<
    TAccountPublisherAuthority,
    TAccountConfig,
    TAccountPublisher,
    TAccountCampaign,
    TAccountClaim,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SubmitClaimInstruction<
    TProgramAddress,
    TAccountPublisherAuthority,
    TAccountConfig,
    TAccountPublisher,
    TAccountCampaign,
    TAccountClaim,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? URTHR_NET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    publisherAuthority: {
      value: input.publisherAuthority ?? null,
      isWritable: true,
    },
    config: { value: input.config ?? null, isWritable: false },
    publisher: { value: input.publisher ?? null, isWritable: true },
    campaign: { value: input.campaign ?? null, isWritable: true },
    claim: { value: input.claim ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.publisher.value) {
    accounts.publisher.value = await findSubmitClaimPublisherPda({
      publisherAuthority: getAddressFromResolvedInstructionAccount(
        "publisherAuthority",
        accounts.publisherAuthority.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("publisherAuthority", accounts.publisherAuthority),
      getAccountMeta("config", accounts.config),
      getAccountMeta("publisher", accounts.publisher),
      getAccountMeta("campaign", accounts.campaign),
      getAccountMeta("claim", accounts.claim),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getSubmitClaimInstructionDataEncoder().encode(
      args as SubmitClaimInstructionDataArgs,
    ),
    programAddress,
  } as SubmitClaimInstruction<
    TProgramAddress,
    TAccountPublisherAuthority,
    TAccountConfig,
    TAccountPublisher,
    TAccountCampaign,
    TAccountClaim,
    TAccountSystemProgram
  >);
}

export type SubmitClaimInput<
  TAccountPublisherAuthority extends string = string,
  TAccountConfig extends string = string,
  TAccountPublisher extends string = string,
  TAccountCampaign extends string = string,
  TAccountClaim extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  publisherAuthority: TransactionSigner<TAccountPublisherAuthority>;
  config: Address<TAccountConfig>;
  publisher: Address<TAccountPublisher>;
  campaign: Address<TAccountCampaign>;
  claim: Address<TAccountClaim>;
  systemProgram?: Address<TAccountSystemProgram>;
  eventCount: SubmitClaimInstructionDataArgs["eventCount"];
  merkleRoot: SubmitClaimInstructionDataArgs["merkleRoot"];
};

export function getSubmitClaimInstruction<
  TAccountPublisherAuthority extends string,
  TAccountConfig extends string,
  TAccountPublisher extends string,
  TAccountCampaign extends string,
  TAccountClaim extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof URTHR_NET_PROGRAM_ADDRESS,
>(
  input: SubmitClaimInput<
    TAccountPublisherAuthority,
    TAccountConfig,
    TAccountPublisher,
    TAccountCampaign,
    TAccountClaim,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): SubmitClaimInstruction<
  TProgramAddress,
  TAccountPublisherAuthority,
  TAccountConfig,
  TAccountPublisher,
  TAccountCampaign,
  TAccountClaim,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? URTHR_NET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    publisherAuthority: {
      value: input.publisherAuthority ?? null,
      isWritable: true,
    },
    config: { value: input.config ?? null, isWritable: false },
    publisher: { value: input.publisher ?? null, isWritable: true },
    campaign: { value: input.campaign ?? null, isWritable: true },
    claim: { value: input.claim ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("publisherAuthority", accounts.publisherAuthority),
      getAccountMeta("config", accounts.config),
      getAccountMeta("publisher", accounts.publisher),
      getAccountMeta("campaign", accounts.campaign),
      getAccountMeta("claim", accounts.claim),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getSubmitClaimInstructionDataEncoder().encode(
      args as SubmitClaimInstructionDataArgs,
    ),
    programAddress,
  } as SubmitClaimInstruction<
    TProgramAddress,
    TAccountPublisherAuthority,
    TAccountConfig,
    TAccountPublisher,
    TAccountCampaign,
    TAccountClaim,
    TAccountSystemProgram
  >);
}

export type ParsedSubmitClaimInstruction<
  TProgram extends string = typeof URTHR_NET_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    publisherAuthority: TAccountMetas[0];
    config: TAccountMetas[1];
    publisher: TAccountMetas[2];
    campaign: TAccountMetas[3];
    claim: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: SubmitClaimInstructionData;
};

export function parseSubmitClaimInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSubmitClaimInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      publisherAuthority: getNextAccount(),
      config: getNextAccount(),
      publisher: getNextAccount(),
      campaign: getNextAccount(),
      claim: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getSubmitClaimInstructionDataDecoder().decode(instruction.data),
  };
}
